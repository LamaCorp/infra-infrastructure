---

- name: Reload tinc
  ansible.builtin.service:
    name: tinc
    state: "{{ tinc_state | default('reloaded') }}"
  throttle: 1

- name: Restart tinc
  ansible.builtin.service:
    name: tinc
    state: "restarted"
  throttle: 1
