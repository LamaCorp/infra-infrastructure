---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gatus
  annotations:
    external-dns.alpha.kubernetes.io/controller: ignored
spec:
  hostnames:
    - status.prologin.org
